import { KarmaBalancePipe } from './karma-balance.pipe';

describe('KarmaBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new KarmaBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
