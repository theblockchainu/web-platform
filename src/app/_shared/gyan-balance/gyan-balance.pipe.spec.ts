import { GyanBalancePipe } from './gyan-balance.pipe';

describe('GyanBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new GyanBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
