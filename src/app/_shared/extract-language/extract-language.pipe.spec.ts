import { ExtractLanguagePipe } from './extract-language.pipe';

describe('ExtractLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
